Task 6: update README.md §1.4 to remove the FAIL clause and replace it
with an honest improvement status (all real T0 eliminated; freq-node binding
and DRAM efficiency now T1; decision-grade still requires T3+).

Date: 2026-07-31

====================================================================
1. Files modified
====================================================================
- README.md, §1.4 only:
    line 67 (introductory note):
        "包含 T0/T1 参数；在 `decision-grade` 证据补齐前，不应用作产品承诺。"
        -> "包含 T1 参数（全部真实 T0 参数已消除）；在 `decision-grade`
            证据补齐前（需全部排名参数达到 T3+），不应用作产品承诺。"
    line 78 (跨节点验证结论 row, decision-grade status field):
        "决策级状态：**FAIL（频率-节点绑定为探索性结论，多节点覆盖不完全）**"
        -> "决策级状态：**已升级，未就绪**。最后一个真实 T0 参数
            `tensor_core_descriptor_overhead` 已升级 T1（源引 ARM PL330 DMA
            PrimeCell 公开手册 DDID0424）；4 个频率-节点条目
            (`max_freq_{7,12,22,28}nm`) 已按 T1 加入，基于公开产品参考与
            TSMC 节点特性，跨节点覆盖由探索性升级为 T1，频率-节点绑定不再
            为探索性结论；3 个 DRAM 效率条目 (`dram_efficiency` /
            `dram_efficiency_random_bw` / `random_latency_penalty_cycles`)
            已按 T1 加入，基于 JEDEC LPDDR5 时序与公开 DRAM 局部性参考。
            决策级仍未达成：需全部排名参数达到 T3+ 权威证据，故不标注为就绪。"
    No other sections touched. Table structure (3 columns, header separator,
    all rows) preserved; scenario A/B numbers untouched.
- README.md, §8.3 基线溯源 (follow-up cleanup, same todo):
    line 386 (dram_efficiency row): stale self-referential note
        "README 中 75% 声称已被证伪（待 Todo 6 统一清理）" removed; replaced with
        "0.85 — conservative sequential-decode baseline (matches
        `references/calibration/parameters.yaml` and `sim/config/npu_config.yaml`)".
    The note claimed Todo 6 should clean up a "75% claim in README", but no such
    claim existed (grep for "75%"/"0.75" found only the note itself).

====================================================================
2. grep: stale FAIL clause / T0 claims (expect NO matches)
====================================================================
$ grep -n "FAIL\|频率-节点绑定为探索性结论\|多节点覆盖不完全\|包含 T0/T1" README.md
(no matches — exit 1)

====================================================================
3. grep: updated text present (expect matches)
====================================================================
$ grep -n "T3+\|T1 参数\|tensor_core_descriptor_overhead\|max_freq\|dram_efficiency" README.md
67:> 以下数值为 Arc Model 在**当前校准假设**下的估算，包含 T1 参数（全部真实 T0 参数已消除）；在 `decision-grade` 证据补齐前（需全部排名参数达到 T3+），不应用作产品承诺。
78:| 跨节点验证结论 | block 引擎面积单调 7→28nm（99→261mm² @lpddr5_3b）；全 8 引擎 × 4 节点排名矩阵揭示 os_systolic 在跨所有节点和场景中均占绝对领先（31.8 tok/s @lpddr5, 310.9 tok/s @onchip），GMMA 在高 BW 场景中作为第二名具竞争力（203.5 tok/s @7nm onchip）；WMMA/GMMA PE 校准已升级 T1（PE 面积 4.5/5.5 mm² @7nm、WMMA 片段序列化 120 cycles、GMMA pipeline，源自 H100 SM die/Volta 架构分析），WMMA 全模型 tok/s 仍低（~0.5 tok/s）但较校准前提升 ~10×（per-FFN_down-GEMM 6.9→67.6 tok/s）；决策级状态：**已升级，未就绪**。最后一个真实 T0 参数 `tensor_core_descriptor_overhead` 已升级 T1（源引 ARM PL330 DMA PrimeCell 公开手册 DDID0424）；4 个频率-节点条目（`max_freq_{7,12,22,28}nm`）已按 T1 加入，基于公开产品参考与 TSMC 节点特性，跨节点覆盖由探索性升级为 T1，频率-节点绑定不再为探索性结论；3 个 DRAM 效率条目（`dram_efficiency`/`dram_efficiency_random_bw`/`random_latency_penalty_cycles`）已按 T1 加入，基于 JEDEC LPDDR5 时序与公开 DRAM 局部性参考。决策级仍未达成：需全部排名参数达到 T3+ 权威证据，故不标注为就绪。 | [`全引擎跨节点排名矩阵`](.omo/evidence/task-4-cross-node-all-engines-dse-matrix.md)；频率感知数据：[`.omo/evidence/investigate-all-engines-cross-node-freq.md`](.omo/evidence/investigate-all-engines-cross-node-freq.md) |
386:| **dram_efficiency** | 0.85 — conservative sequential-decode baseline (matches `references/calibration/parameters.yaml` and `sim/config/npu_config.yaml`) |

Note: the line-386 hit is §8.3 基线溯源. The pre-existing stale
self-referential note ("README 中 75% 声称已被证伪（待 Todo 6 统一清理）") was
removed as a follow-up within this todo: grep for "75%" / "0.75" across
README.md found ONLY that note itself, i.e. the 75% claim it referenced did
not exist. Cell now points at the two canonical sources instead.

====================================================================
3b. grep: stale §8.3 note removed (expect NO matches)
====================================================================
$ grep -n "75%\|待 Todo 6 统一清理" README.md
(no matches — exit 1)

====================================================================
4. Table integrity
====================================================================
README.md lines 65-79 re-read after edit: header separator row present
(line 70), every row has exactly 3 columns, 跨节点验证结论 cell is a single
column with no unescaped pipe, scenario A/B area/decode/TTFT values unchanged
(61mm²/23 tok/s/45ms vs 66mm²/148 tok/s/160ms). Evidence-file references
(task-4-cross-node-all-engines-dse-matrix.md,
investigate-all-engines-cross-node-freq.md) kept.

====================================================================
5. Conclusion
====================================================================
README §1.4 no longer claims T0/T1 parameters (says T1, all real T0
eliminated), no FAIL clause remains, replacement status is honest about
improvement (tensor_core_descriptor_overhead T0->T1 via ARM PL330 DDID0424,
4 freq-node entries T1 via public product refs + TSMC node chars, 3 DRAM
entries T1 via JEDEC LPDDR5 + public DRAM-locality refs) and about the
remaining limit (decision-grade not achieved; requires T3+ authoritative
evidence for all ranking parameters). NOT labeled PASS.
§8.3 stale self-referential note (75% claim / 待 Todo 6 统一清理) removed;
grep -n "75%\|待 Todo 6 统一清理" README.md -> no matches (exit 1).
