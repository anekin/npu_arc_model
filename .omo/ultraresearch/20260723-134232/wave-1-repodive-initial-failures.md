# Wave 1 Open-Source Deep-Dive Returns

The `oss_openvla` and `oss_small_vla` workers failed before producing repository findings because the local librarian response stream disconnected.

Action: both scopes were folded into `retry_vla_models`; direct repository inspection remains available for contested model-shape or action-chunk claims.

## EXPAND

- LEAD: executable VLA defaults and edge inference paths — WHY: papers omit deployment details — ANGLE: inspect canonical repositories at pinned SHAs
